const intialState = {
  loading: false,
  currentUser: null,
  error: null,
};

export default (state = intialState, { type, payload }) => {
  switch (type) {
      
    default:
      return state;
  }
};
